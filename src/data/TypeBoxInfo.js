export default class TypeBoxInfo {
  constructor(text, forwardSpeed, waitAfterDone, backSpeed) {
    this.text = text
    this.forwardSpeed = forwardSpeed
    this.waitAfterDone = waitAfterDone
    this.backSpeed = backSpeed
  }
}
